export const splitHeroTitle = (
  titleStart: string,
  titleAccent: string,
  titleEnd: string,
): { start: string; accent: string; end: string } => ({
  start: titleStart,
  accent: titleAccent,
  end: titleEnd,
})
